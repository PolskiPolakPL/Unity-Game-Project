using UnityEngine;
using UnityEngine.AI;

public class TitleAIMovementScript : MonoBehaviour
{
    [SerializeField] Transform target;
    NavMeshAgent agent;
    // Start is called before the first frame update
    void Start()
    {
    }
}
